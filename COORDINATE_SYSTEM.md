# ⚠️ КООРДИНАТНАЯ СИСТЕМА - РЕАЛЬНОСТЬ

**ВАЖНО**: Эта информация подтверждена экспериментально! Не менять!

---

## 🧭 Координаты

### Оси:
- **X растёт на ВОСТОК** (влево, если смотреть на карту с севером вверху)
- **Y растёт на СЕВЕР** (вверх)

### Стартовая позиция (карман):
```
START: pos=(0.00, 0.00) th=0.0°
- Робот в кармане
- Смотрит на СЕВЕР (th=0°, внутрь кармана)
```

### Углы поворота:
```
th = 0°   → СЕВЕР ↑
th = 90°  → ВОСТОК ← (X+)
th = 180° → ЮГ ↓
th = 270° → ЗАПАД → (X-)
```

### Движение назад из кармана:
```
Команда: ↓ ↓ ↓ (3 раза по 0.5м = 1.5м назад)
Результат: pos=(-1.5, 0.0, 0°)
           X уменьшился! (двигались на ЗАПАД, задом)
```

---

## 📡 LIDAR - НАЗВАНИЯ ПЕРЕПУТАНЫ!

**КРИТИЧНО**: В телеметрии названия L и R **перепутаны местами**!

### Реальность:
- **L** (left в коде) → измеряет расстояние **СПРАВА** по курсу
- **R** (right в коде) → измеряет расстояние **СЛЕВА** по курсу
- **F** (front) → расстояние **ВПЕРЕДИ** (правильно)

### Визуализация:

```
Робот смотрит ВПЕРЁД (th=любой):

             F (front)
               ↑
               |
               |
    L ---> [РОБОТ] <--- R
  (измеряет  █████  (измеряет
   СПРАВА)          СЛЕВА)

   Реальная    Реальная
   стена       стена
   СПРАВА      СЛЕВА
```

### Пример 1: Коридор СЕВЕР-ЮГ, робот едет на СЕВЕР

```
Телеметрия: F=5.0  L=1.5  R=2.0

Реальность:
         F=5.0м впереди свободно
           ↑
           |
  стена    █    стена
  1.5м --> █ <-- 2.0м
 (СПРАВА) (РОБОТ) (СЛЕВА)

Вывод: Робот смещён к ПРАВОЙ стене (ближе на 0.5м)
```

### Пример 2: Центрирование в коридоре

```python
def center_robot_in_corridor():
    tel = recv_tel()
    pose, vel, gyro, lidar = tel
    F, L, R = extract_lidar(lidar)

    # ВНИМАНИЕ: L=СПРАВА, R=СЛЕВА!

    if L < R - 0.15:
        # ПРАВАЯ стена ближе → робот смещён вправо
        # Нужно повернуть ВЛЕВО
        turn_left(math.pi/64)
        print(f"Коррекция: смещён вправо, L={L:.2f} < R={R:.2f}")

    elif R < L - 0.15:
        # ЛЕВАЯ стена ближе → робот смещён влево
        # Нужно повернуть ВПРАВО
        turn_right(math.pi/64)
        print(f"Коррекция: смещён влево, R={R:.2f} < L={L:.2f}")

    else:
        # |L - R| < 0.15м → робот по центру
        print(f"По центру: L={L:.2f} ≈ R={R:.2f}")
```

---

## 🔄 Движение и повороты

### Движение ВПЕРЁД (↑):
```python
move_forward(0.5)  # Двигаться вперёд 0.5м по направлению th
```

**Если th=0° (СЕВЕР)**:
- X не меняется
- Y увеличивается (+0.5)

**Если th=90° (ВОСТОК)**:
- X увеличивается (+0.5) — движемся на ВОСТОК
- Y не меняется

**Если th=180° (ЮГ)**:
- X не меняется
- Y уменьшается (-0.5)

### Движение НАЗАД (↓):
```python
move_backward(0.5)  # Двигаться назад 0.5м
```

**Если th=0° (СЕВЕР)**:
- X не меняется
- Y уменьшается (-0.5) — едем задом на ЮГ

### Повороты:

**Поворот ВПРАВО (→)** — по часовой стрелке:
```python
turn_angle(-math.pi/16)  # -11.25° = вправо (CW)
# th увеличивается: 0° → 11.25° → 22.5° ...
```

**Поворот ВЛЕВО (←)** — против часовой:
```python
turn_angle(+math.pi/16)  # +11.25° = влево (CCW)
# th уменьшается: 0° → -11.25° → -22.5° ...
```

---

## 🗺️ Маршрут (ключевые точки)

### Узел 1: START (карман)
```
pos=(0.00, 0.00) th=0.0°
Смотрим на СЕВЕР (в глубину кармана)
```

### Узел 2: Выезд из кармана (задом)
```
Команды: ↓ ↓ ↓ ↓ ↓ ↓ (6 шагов назад)
Результат: pos≈(-3.0, 0.0, 0°)
X уменьшился → двигались на ЗАПАД
```

### Узел 3: Поворот на ВОСТОК
```
Команды: → → → → → → → → (8 раз = 90°)
Результат: pos≈(-3.0, 0.0, 90°)
Смотрим на ВОСТОК (X+)
```

### Узел 4: Движение на ВОСТОК (мимо уток)
```
Команды: ↑ ↑ ↑ ↑ (4 шага вперёд по 0.5м = 2м)
Результат: pos≈(-3.0, 2.0, 90°)
Y увеличился → двигались на ВОСТОК (вдоль оси Y при th=90°)

ОШИБКА! Должно быть:
X увеличился → pos≈(-1.0, 0.0, 90°)
```

**Внимание**: Координаты начинают врать уже здесь!

---

## 🧪 Как проверить правильность координат по LIDAR

### Метод 1: Параллельные стены (коридор)

Если едешь по коридору СЕВЕР-ЮГ и **L ≈ R** → робот **параллелен стенам**.

**Но телеметрия может показывать неправильный th!**

```python
# Проверка по LIDAR:
tel = recv_tel()
F, L, R = extract_lidar(tel)

if abs(L - R) < 0.1:
    print("Робот параллелен стенам (LIDAR)")
    # Реальный угол: 0° или 180° (вдоль коридора)
else:
    print(f"Робот НЕ параллелен, L={L:.2f} R={R:.2f}")
```

### Метод 2: Известные препятствия

Если знаешь, что утка на расстоянии 2м СПРАВА:

```python
tel = recv_tel()
F, L, R = extract_lidar(tel)

if L ≈ 2.0:  # L измеряет СПРАВА!
    print("Утка справа, координаты правильные!")
else:
    print(f"Ожидали L=2.0, получили L={L:.2f} — ОШИБКА!")
```

---

## ⚠️ КРИТИЧНЫЕ ОШИБКИ (НЕ ДЕЛАТЬ!)

### ❌ НЕПРАВИЛЬНО:
```python
# ОШИБКА: перепутали L и R
if L < R:
    print("Робот ближе к левой стене")  # НЕВЕРНО!
```

### ✅ ПРАВИЛЬНО:
```python
# L измеряет СПРАВА, R измеряет СЛЕВА
if L < R:
    print("Робот ближе к ПРАВОЙ стене")  # ВЕРНО!
```

---

### ❌ НЕПРАВИЛЬНО:
```python
# ОШИБКА: движение на ВОСТОК уменьшает X
if dx < 0:
    print("Двигались на запад")
```

### ✅ ПРАВИЛЬНО:
```python
# X растёт на ВОСТОК
if dx > 0:
    print("Двигались на ВОСТОК (X+)")
if dx < 0:
    print("Двигались на ЗАПАД (X-)")
```

---

## 📐 Формулы

### Расстояние между точками:
```python
dx = x2 - x1
dy = y2 - y1
distance = math.sqrt(dx*dx + dy*dy)
```

### Угол к цели:
```python
dx = target_x - current_x
dy = target_y - current_y
target_angle = math.atan2(dy, dx)  # радианы
target_angle_deg = math.degrees(target_angle)
```

**Пример**:
```python
# Текущая позиция: (0, 0), цель: (1, 0)
dx = 1, dy = 0
target_angle = atan2(0, 1) = 0 радиан = 0°
# НЕТ! Ошибка! Цель на ВОСТОКЕ (X+), должен быть угол 90°!
```

**ПРАВИЛЬНАЯ формула** (с учётом координатной системы):
```python
# Пересчёт: atan2 даёт угол от оси X,
# а th=0° у нас СЕВЕР (ось Y)
angle_from_x = math.atan2(dy, dx)
th_target = angle_from_x + 90°  # Сдвиг системы координат
```

**ИЛИ:**
```python
# Использовать стандартную формулу, но поменять оси:
# В нашей системе X→ВОСТОК, Y→СЕВЕР
# В математике: X→ВОСТОК, Y→СЕВЕР (то же самое!)
# Но th=0° у нас СЕВЕР, а в математике 0° = ВОСТОК

# Поэтому:
math_angle = math.atan2(dy, dx)  # Угол в математической системе
robot_angle = math_angle - 90°   # Пересчёт в систему робота
```

---

## 🎯 Итого: Что ТОЧНО знаем

1. **X+ = ВОСТОК** (влево на карте)
2. **Y+ = СЕВЕР** (вверх на карте)
3. **th=0° = СЕВЕР** (смотрит вверх)
4. **th=90° = ВОСТОК** (смотрит влево на карте)
5. **L = СПРАВА** (по курсу робота)
6. **R = СЛЕВА** (по курсу робота)
7. **F = ВПЕРЕДИ** (правильно)

**Это экспериментально подтверждено! Не менять!**

---

**Дата**: 2025-10-03
**Источник**: Эксперименты с роботом, step.log (2 трека)
